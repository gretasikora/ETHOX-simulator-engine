# Core LLM and simulation dependencies
openai>=1.0
numpy>=1.24

# Personality sampling dependencies (personalities/sampling.py)
pandas>=2.0
statsmodels>=0.14
pyreadr>=0.4  # Optional: only needed if loading .rda files
