[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd backend && gunicorn societyviz.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120"
